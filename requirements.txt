# Core dependencies
python-telegram-bot==21.6       # Async Telegram bot framework (v20+ uses asyncio)
httpx>=0.27.0                   # Async HTTP client for Ollama API
python-dotenv==1.0.1            # Load .env into os.environ
aiosqlite>=0.20.0               # Async SQLite driver for conversation memory
